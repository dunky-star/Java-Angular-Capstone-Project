import { DunkyCustomValidators } from './dunky-custom-validators';

describe('DunkyCustomValidators', () => {
  it('should create an instance', () => {
    expect(new DunkyCustomValidators()).toBeTruthy();
  });
});
